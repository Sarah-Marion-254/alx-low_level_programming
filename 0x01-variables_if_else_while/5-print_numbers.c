#include <stdio.h>

/**
 * main - This marks the start of code
 *
 * Return: Returns 0 if Success
 */
int main(void)
{
	int  m;


	for (m = 0; m < 10; m++)
	{
		printf("%d", m);
	}
	printf("\n");
	return (0);
}
